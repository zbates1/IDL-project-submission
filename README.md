# Sim-to-Real Transfer for CV Mechanism Classification

CMU 11-785 Intro to Deep Learning — Spring 2026.

## Setup

```bash
pip install -r requirements.txt
```

## Data

Simulated training data (`simulated_cvs.npz`, ~290 MB) is not included. Contact the authors or regenerate via the simulation pipeline.

Real experimental CVs are included at `data/real/processed/real_cvs.npz`.

## Run

Open `notebooks/cv_mechanism_classification.ipynb` and execute.

## Authors

Zane Bates, Karl Bates
